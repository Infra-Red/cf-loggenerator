<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <!--<encoder>
            <pattern>%d{ISO8601} %p %logger - %m%n</pattern>
        </encoder>-->

        <!--encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <fieldNames>
                <timestamp>log_time</timestamp>
                <level>log_level</level>
                <logger>log_class</logger>
                <thread>log_thread</thread>
                <message>log_message</message>
                <stackTrace>log_stacktrace</stackTrace>
                <version>[ignore]</version>
                <levelValue>[ignore]</levelValue>
            </fieldNames>

            <jsonFactoryDecorator class="com.altoros.CustomEscapeJsonFactoryDecorator"/>
        </encoder-->

        <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
            <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter"/>

            <includeTimestamp>true</includeTimestamp>
            <timestampFormat>yyyy-MM-dd HH:mm:ss.SSS</timestampFormat>
            <timestampFormatTimezoneId>UTC</timestampFormatTimezoneId>
            <appendLineSeparator>true</appendLineSeparator>
            <includeLevel>true</includeLevel>
            <includeLoggerName>true</includeLoggerName>
            <includeFormattedMessage>true</includeFormattedMessage>
            <includeException>true</includeException>

            <includeThreadName>true</includeThreadName>
            <includeContextName>false</includeContextName>
        </layout>

    </appender>

    <!-- Strictly speaking, the level attribute is not necessary since -->
    <!-- the level of the root level is set to DEBUG by default.       -->
    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
